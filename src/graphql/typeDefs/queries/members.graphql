type MembersQuery {
  all: [PublicProfile]
  member(slug: String): PublicProfile
  me: Profile @auth(requires: "members")
  isProfileSlugTaken(slug: String!): Boolean @auth(requires: "members")
  members(pageSize: Int, after: String): MemberConnection
}
