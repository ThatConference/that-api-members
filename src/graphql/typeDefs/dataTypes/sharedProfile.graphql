type SharedProfile @key(fields: "id") {
  "unique profile id"
  id: ID!
  "member first name"
  firstName: String!
  "member last name"
  lastName: String!
  "member email"
  email: EmailAddress!
  "Member's active partner. Used when entering partner/us path"
  activePartnerId: ID
}
