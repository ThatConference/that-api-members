input NotificationPreferencesCreateInput {
  "Subscribe to Meet THAT (default: false)"
  meetThatCamper: Boolean
  "Subscribe to profile updates (default: true)"
  profileUpdates: Boolean
}

input NotificationPreferencesUpdateInput {
  "Subscribe to Meet THAT (default: false)"
  meetThatCamper: Boolean
  "Subscribe to profile updates (default: true)"
  profileUpdates: Boolean
}
